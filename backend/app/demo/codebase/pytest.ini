[pytest]
pythonpath = .
testpaths = tests
markers =
    req(id): trace a test to an automotive software requirement ID (ISO 26262)
    level(name): test execution level - UNIT, SIL, or HIL
    asil(rating): ASIL safety classification - A, B, C, or D
